\documentclass{article}
\usepackage[top=1.5cm, bottom=1.5cm, left=2cm, right=1.5cm]{geometry}
\usepackage[hidelinks,unicode]{hyperref}
\usepackage[dethi]{ex_test} 
\input{khaibao.tex}
% \hienloigiai
% \anloigiai 
\usepackage{polyglossia}
\setmainlanguage{vietnamese}
\usepackage{fontspec}
\setmainfont{Times New Roman}

% ==================================================
% === PREMIUM DESIGN DEFINITIONS ===
% ==================================================
\usepackage{xparse}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable,hooks,raster}
\usepackage{tikz}
\usetikzlibrary{positioning,calc}
\usepackage[explicit]{titlesec}
\usepackage{fontawesome}
\usepackage{amssymb} % Required for \mathbb

% Colors
\definecolor{mainColor}{HTML}{005f73}
\definecolor{secondColor}{HTML}{0a9396}
\definecolor{accentColor}{HTML}{94d2bd}
\definecolor{highlightColor}{HTML}{e9d8a6}
\definecolor{alertColor}{HTML}{ae2012}

% Styles
\tcbset{
    premiumbox/.style={
        enhanced,
        colback=white,
        colframe=#1,
        fonttitle=\bfseries,
        attach boxed title to top left={yshift*=-\tcboxedtitleheight/2, xshift=5mm},
        boxed title style={colback=#1, rounded corners},
        drop fuzzy shadow
    }
}

% Define Environments explicitly
% Using DeclareDocumentEnvironment to handle both new and renew cases
\newtcolorbox[auto counter, number within=section]{dn_inner}[3][]{
    premiumbox=secondColor,
    title={\textbf{Định nghĩa~\thetcbcounter:} #2},
    label={#3},
    #1
}
\DeclareDocumentEnvironment{dn}{ m m }
{\begin{dn_inner}{#1}{#2}}
{\end{dn_inner}}

\newtcolorbox[auto counter, number within=section]{dl_inner}[3][]{
    premiumbox=mainColor,
    title={\textbf{Định lý~\thetcbcounter:} #2},
    label={#3},
    #1
}
\DeclareDocumentEnvironment{dl}{ m m }
{\begin{dl_inner}{#1}{#2}}
{\end{dl_inner}}

\newtcolorbox[auto counter, number within=section]{tc_inner}[3][]{
    premiumbox=accentColor!80!black,
    title={\textbf{Tính chất~\thetcbcounter:} #2},
    label={#3},
    #1
}
\DeclareDocumentEnvironment{tc}{ m m }
{\begin{tc_inner}{#1}{#2}}
{\end{tc_inner}}

% Helper to initialize ex-like behavior
\newcommand{\InitPremiumEx}{
    \setcounter{dapan}{0}
    \setcounter{numTrueSol}{0}
    \gdef\saveFileAns{}
    \gdef\keyEX{}
    \gdef\chType{0}
    \def\dotEX{.}
    \SetExEnvironmentOptions
}

\newtcolorbox[auto counter, number within=section]{bt_inner}[3][]{
    premiumbox=mainColor,
    title={\textbf{Bài tập~\thetcbcounter:} #2},
    label={#3},
    #1
}
\DeclareDocumentEnvironment{bt}{ m m }
{\begin{bt_inner}{#1}{#2}\InitPremiumEx}
{\end{bt_inner}}

\newtcolorbox[auto counter, number within=section]{vd_inner}[3][]{
    premiumbox=secondColor,
    title={\textbf{Ví dụ~\thetcbcounter:} #2},
    label={#3},
    #1
}
\DeclareDocumentEnvironment{vd}{ m m }
{\begin{vd_inner}{#1}{#2}\InitPremiumEx}
{\end{vd_inner}}

% Note (No counter)
\DeclareDocumentEnvironment{note}{ }
{
    \begin{tcolorbox}[
        enhanced,
        colback=highlightColor!10,
        colframe=alertColor,
        borderline west={4pt}{0pt}{alertColor},
        frame hidden,
        left=10pt,
        title={\textbf{\faExclamationTriangle\ Chú ý}},
        coltitle=alertColor,
        attach boxed title to top left={yshift=-2mm, xshift=2mm},
        boxed title style={empty, boxsep=0pt},
        fonttitle=\bfseries
    ]
}
{
    \end{tcolorbox}
}

% Dang toan (No counter, custom title)
\DeclareDocumentEnvironment{dang}{ O{} }
{
    \begin{tcolorbox}[
        enhanced,
        colback=white,
        colframe=mainColor,
        fonttitle=\bfseries\large,
        title={#1},
        attach boxed title to top left={yshift=-2mm, xshift=2mm},
        boxed title style={colback=mainColor, sharp corners},
        sharp corners,
        top=4mm,
        drop fuzzy shadow
    ]
}
{
    \end{tcolorbox}
}

% Section Styling
\titleformat{\section}[block]
  {\normalfont\Large\bfseries\color{mainColor}}
  {}
  {0pt}
  {%
    \begin{tikzpicture}[baseline=(current bounding box.west)]
      \node[
        fill=mainColor,
        text=white,
        rounded corners=3pt,
        inner sep=5pt,
        font=\Large\bfseries
      ] (num) {\thesection};
      \node[
        right=5pt of num,
        text=mainColor,
        font=\Large\bfseries,
        align=left
      ] (title) {\MakeUppercase{#1}};
      \draw[mainColor, line width=1.5pt] (title.south west) -- (title.south east);
    \end{tikzpicture}%
  }

\titleformat{\subsection}[block]
  {\normalfont\large\bfseries\color{secondColor}}
  {}
  {0pt}
  {%
    \begin{tikzpicture}[baseline=(current bounding box.west)]
      \node[
        text=secondColor,
        font=\large\bfseries,
        inner sep=0pt,
        align=left
      ] (title) {\thesubsection.~#1};
      \draw[secondColor!50, line width=1pt] (title.south west) -- ([xshift=2cm]title.south west);
    \end{tikzpicture}%
  }

\begin{document}

\section{Chương 1: Hàm Số Lũy Thừa}
\subsection{Lý thuyết cơ bản}

\begin{dn}{Hàm số lũy thừa}{dn:luythua}
    Hàm số $y=x^\alpha$ với $\alpha \in \mathbb{R}$ được gọi là hàm số lũy thừa.
\end{dn}

\begin{dl}{Đạo hàm}{dl:daoham}
    $(x^\alpha)' = \alpha x^{\alpha-1}$.
\end{dl}

\subsection{Bài tập vận dụng}

\begin{vd}{Tìm tập xác định}{}
    Tìm tập xác định của hàm số $y=(x-1)^{\frac{1}{3}}$.
\end{vd}

\begin{bt}{Tính đạo hàm}{}
    Tính đạo hàm của $y=x^{\pi}$.
    \choiceTF
	{$\dfrac{9}{\sqrt{35}}$}
	{$-\dfrac{9}{\sqrt{35}}$}
	{$-\dfrac{9}{2\sqrt{35}}$}
	{\True $\dfrac{9}{2\sqrt{35}}$}
	\loigiai{
		Ta có $\overrightarrow{AB} = (1;5;-2)$, $\overrightarrow{AC} = (5;4;-1)$.\\
		Do đó
		$$ \cos \widehat{BAC} = \dfrac{\overrightarrow{AB}\cdot\overrightarrow{AC}}{\left|\overrightarrow{AB} \right|\cdot \left| \overrightarrow{AC}\right|} = \dfrac{5 + 20 + 2}{\sqrt{30}\cdot\sqrt{42}} = \dfrac{9}{2\sqrt{35}}. $$
	}
\end{bt}

\section{Chương 2: Mũ và Logarit}
\subsection{Phương trình mũ}

\begin{dang}[Phương trình cơ bản]
    $a^x = b \Leftrightarrow x = \log_a b$.
\end{dang}

\begin{note}
    Điều kiện: $0 < a \ne 1, b > 0$.
\end{note}

\end{document}
